services:
  - type: web
    name: pitch-visualizer-pro
    runtime: docker # Changed to Docker
    envVars:
      - key: HF_API_TOKEN
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: MOCK_IMAGE_GEN
        value: "False"
      - key: MODEL_ID
        value: "black-forest-labs/FLUX.1-schnell"
